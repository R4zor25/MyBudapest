id: welovebudapest
name: We Love Budapest
enabled: false
priority: 30
fetcher: http
rate_limit_seconds: 2
plugin: null

# PLACEHOLDER — deliberately empty, do not guess selectors here.
#
# RE-CHECKED 2026-08-22 for the aggregator batch, which asked for this source again: the
# rule is unchanged and the decision below stands. The only request made was for
# robots.txt itself — which is the mechanism for asking permission, not a way around the
# answer. No listing page was fetched and no fixture exists for this source, deliberately.
#
# welovebudapest.com/robots.txt explicitly disallows the "anthropic-ai" user agent
# site-wide (first checked 2026-08-16):
#
#   User-agent: anthropic-ai
#   Disallow: /
#
# This entry names Anthropic specifically, alongside GPTBot, ChatGPT-User,
# Google-Extended, CCBot and PerplexityBot. /programok/ itself is not disallowed under
# `User-agent: *`, so a differently-named crawler could technically fetch it — but the
# site has clearly stated it does not want an Anthropic model doing that, and this
# fixture would have been fetched by exactly one, under a project-specific User-Agent
# chosen precisely to be distinct from that name. Presenting under a different string to
# route around a rule that plainly targets what I am is not something to do quietly, so
# no fixture was fetched and no selectors were written (SPEC 6.1 step 5: "blokkol vagy
# hetente törik? dobd a forrást" — this is exactly that stop condition, applied before
# even reaching the technical fetch).
listing:
  urls: []
